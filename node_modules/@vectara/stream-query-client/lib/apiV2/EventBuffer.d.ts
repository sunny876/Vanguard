export declare class EventBuffer {
    private onStreamEvent;
    private includeRaw;
    private status;
    private events;
    private eventInProgress;
    private updatedText;
    constructor(onStreamEvent: (event: any) => void, includeRaw?: boolean, status?: number);
    consumeChunk(chunk: string): void;
    private enqueueEvent;
    private drainEvents;
}
